# Copy to .env (optional) or set environment variables directly.
# For Windows PowerShell, see README.md for commands.

# A random 64-hex secret; generate one for production.
SECRET_KEY=replace-with-a-secure-random-hex

# Option A: set plaintext admin password (hash generated at startup)
ADMIN_PASSWORD=changeme

# Option B: set a precomputed hash instead (overrides ADMIN_PASSWORD)
# ADMIN_PASSWORD_HASH=pbkdf2:sha256:600000$...

# Provider auth (for /provider). If not set, provider uses the admin password.
# PROVIDER_PASSWORD=changeme
# PROVIDER_PASSWORD_HASH=pbkdf2:sha256:600000$...
